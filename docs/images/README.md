# Images

Screenshots and other visual assets referenced by the top-level `README.md`.

- `dashboard.png` — a screenshot of the live dashboard (`http://127.0.0.1:5785`).
  Capture it from a real KDE Plasma session after `./install.sh` has been running
  long enough to have some spans to show, then drop the PNG in here. The README
  already links to this path.
